# Example environment variables for the Discord bot
# Copy this file to .env and fill in the values before running the bot.

# Discord Bot Configuration
DISCORD_TOKEN=your-bot-token-here

# LangFlow API Endpoints
CARD_API_URL=http://localhost:8000/card
ARCHETYPE_API_URL=http://localhost:8000/archetype
POKEMON_API_URL=http://localhost:8000/pokemon
SEARCH_API_URL=http://localhost:8000/search

# LangFlow API Key (if required)
LANGFLOW_API_KEY=your-langflow-api-key

# MongoDB Atlas Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
DB_NAME=card_store

# MongoDB API Server Port
PORT=3000

# MongoDB API URL (for card artwork and semantic search)
MONGODB_API_URL=http://localhost:3000

# Tier List API (optional)
TIER_LIST_API_URL=http://localhost:8000/tierlist

# Notes:
# - This bot uses the Message Content intent to detect messages containing '/card'.
#   In the Discord Developer Portal > Applications > Your App > Bot, enable "Message Content Intent".
# - If your bot is in many guilds or you prefer, consider implementing a slash command
#   instead of reading message content (recommended for production).
# 
# Available Commands:
# /card - Search for a specific card by name
# /archetype - Search for cards in an archetype
# /pokemon - Search for Pokemon information
# /tierlist - Get current tier list (admin only)
# /status - Check bot status and queue (admin only)
# /art - Get card artwork with size options
# /search - Semantic search across all card fields (NEW!)
